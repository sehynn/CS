# CPU 기초

## 1. CPU 구성 요소

- **ALU(산술논리연산장치)**: 산술 연산과 논리 연산을 수행한다.
- **제어장치(Control Unit)**: 명령어를 해석하고 각 장치의 동작을 제어한다.
- **레지스터(Register)**: CPU 내부에서 데이터와 명령어를 임시로 보관하는 초고속 저장 공간이다.

## 2. 주요 레지스터

- **PC(Program Counter)**: 다음에 실행할 명령어의 주소를 저장한다.
- **IR(Instruction Register)**: 현재 실행 중인 명령어를 저장한다.
- **MAR(Memory Address Register)**: 접근할 메모리의 주소를 저장한다.
- **MBR(Memory Buffer Register)**: 메모리에서 읽거나 메모리에 쓸 데이터를 저장한다.

## 3. 명령어 처리 사이클

1. **Fetch**: 메모리에서 실행할 명령어를 가져온다.
2. **Decode**: 가져온 명령어를 해석한다.
3. **Execute**: 해석한 명령어를 실행한다.
4. **Store**: 실행 결과를 레지스터나 메모리에 저장한다.

## 4. CPU 성능 관련 개념

- **클럭(Clock)**: CPU가 작업을 수행하는 기본 주기이다.
- **코어(Core)**: 명령어를 독립적으로 처리할 수 있는 CPU의 핵심 연산 장치이다.
- **스레드(Thread)**: CPU가 처리하는 작업의 실행 단위이다.
- **파이프라이닝(Pipelining)**: 여러 명령어의 처리 단계를 겹쳐 실행해 처리량을 높이는 방식이다.
- **분기 예측(Branch Prediction)**: 조건문의 실행 경로를 미리 예측해 CPU의 대기 시간을 줄이는 기술이다.
